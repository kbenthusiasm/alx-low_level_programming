#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the secone number to multiply with the first number
 * Return: the result of muptiplication between number one and number two
 */

int mul(int x, int y)
{
	return (x * y);
}

